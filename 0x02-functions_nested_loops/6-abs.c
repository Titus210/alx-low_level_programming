#include "main.h"
/**
 *_abs - computes the absolute value of an integer.
 *@r:declared int
 *Return: absolute value of r
 */
int _abs(int r)
{
	if (r >= 1)
	{
		return (r);
	}
	else
	{
		return (r * -1);
	}
}
