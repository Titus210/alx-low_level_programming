#include <stdio.h>
/**
 * main - prints the alphabet in lowercase without q&e
 * Return: 0 (when success)
 */
int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return (0);
}
