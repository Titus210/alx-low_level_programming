#include <stdio.h>
/** 
 * main - program that prints the alphabet in lowercase
 * Return: 0 when success
*/
int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return (0);
}
