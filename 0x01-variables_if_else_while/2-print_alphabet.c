#include <stdio.h>
/**
 * main- program that prints the alphabet in lowercase, followed by a new line.
 * Return 0 if succesfull
 */
int main(void)
{
	char lt;

	for (lt = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
