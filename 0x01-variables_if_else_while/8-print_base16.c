#include <stdio.h>
/**
 * main - program that prints all the numbers of base 16 in lowercase
 * Return: 0(success)
 */
int main(void)
{
	int i = '0';
	char ch = 'a';

	while (i <= '0')
	{
		putchar(i);
		i++;
	}
	while (ch <= 'a')
	{
		putchar(ch); 
		ch++;
	}
	putchar('\n');
	return (0);
}

